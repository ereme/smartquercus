{% extends 'base.html.twig' %}

{% block title %}Opina{% endblock %}

{% block body %}
    <h1>Opina</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ opina.id }}</td>
            </tr>
            <tr>
                <th>Pregunta</th>
                <td>{{ opina.pregunta }}</td>
            </tr>
            <tr>
                <th>Votosfavor</th>
                <td>{{ opina.votosfavor }}</td>
            </tr>
            <tr>
                <th>Votoscontra</th>
                <td>{{ opina.votoscontra }}</td>
            </tr>
            <tr>
                <th>Fechahoralimite</th>
                <td>{{ opina.fechahoralimite ? opina.fechahoralimite|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('opina_index') }}">back to list</a>

    <a href="{{ path('opina_edit', {'id': opina.id}) }}">edit</a>

    {{ include('opina/_delete_form.html.twig') }}
{% endblock %}
