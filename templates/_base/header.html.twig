<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
  <div class="container-fluid">
  <a class="navbar-brand" href="{{ path('inicio') }}">
    <img src="https://getbootstrap.com/docs/4.1/assets/brand/bootstrap-solid.svg" width="30" height="30" class="d-inline-block align-top" alt="">
    {{ brand_name }}
  </a>    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarResponsive">
      <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <a class="nav-link" href="{{ path('inicio') }}">Noticias</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('tratamiento_index') }}">Avisos</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('salud_index') }}">Salud</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('tratamiento_index') }}">Vecinos</a>
          </li>
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('opina_index') }}">Opina</a>
          </li>
        {% else %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('login') }}">Opina</a>
          </li>
        {% endif %}
      </ul>
      <span class="navbar-text">
        {{ app.user.email|default('') }}
    </span>
    </div>
  </div>
</nav>