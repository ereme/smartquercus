{% extends 'base.html.twig' %}

{% block title %}Equipo{% endblock %}

{% block body %}
    <h1>Equipo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipo.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ equipo.numero }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ equipo.nombre }}</td>
            </tr>
            <tr>
                <th>Capacidad</th>
                <td>{{ equipo.capacidad }}</td>
            </tr>
            <tr>
                <th>Roma</th>
                <td>{{ equipo.roma }}</td>
            </tr>
            <tr>
                <th>FechaAdquisicion</th>
                <td>{{ equipo.fechaAdquisicion ? equipo.fechaAdquisicion|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>FechaUltimaInspeccion</th>
                <td>{{ equipo.fechaUltimaInspeccion ? equipo.fechaUltimaInspeccion|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('equipo_index') }}">back to list</a>

    <a href="{{ path('equipo_edit', {'id': equipo.id}) }}">edit</a>

    {{ include('equipo/_delete_form.html.twig') }}
{% endblock %}
