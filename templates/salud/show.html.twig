{% extends 'base.html.twig' %}

{% block title %}Saludables{% endblock %}

{% block body %}

    <div class="container container-fluid">
        {{ salud.fechahora ? salud.fechahora|date('Y-m-d H:i:s') : '' }}
        <div class="row">        
            <h1>{{ salud.titulo }}</h1> 
            <img class="card-img-top" src="{{ asset('images/bienestar1.png') }}" alt="Card image cap">     
            <p>{{ salud.texto }}</p>          
        </div>
        <a href="{{ path('salud_index') }}">Lista</a>
        <a href="{{ path('salud_edit', {'id': salud.id}) }}">Editar</a>
        {{ include('salud/_delete_form.html.twig') }}
    </div> 

{% endblock %}
