{{ form_start(form) }}
	<div class="row">
		<div class="col-md-12">
			{{ form_row(form.titulo) }}
		</div>		
		<div class="col-md-12">
			{{ form_row(form.texto) }}
		</div>	
		<div class="col-md-12">
			{{ form_row(form.fechahora) }}
		</div>	
	</div>	
	<div class="row">
		<div class="col-md-12">
			{{ form_row(form.save) }}
		</div>
	</div>		
{{ form_end(form) }}

