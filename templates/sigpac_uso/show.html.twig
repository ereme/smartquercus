{% extends 'base.html.twig' %}

{% block title %}SigpacUso{% endblock %}

{% block body %}
    <h1>SigpacUso</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sigpac_uso.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ sigpac_uso.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sigpac_uso_index') }}">back to list</a>

    <a href="{{ path('sigpac_uso_edit', {'id': sigpac_uso.id}) }}">edit</a>

    {{ include('sigpac_uso/_delete_form.html.twig') }}
{% endblock %}
