{% extends 'base.html.twig' %}

{% block title %}Explotacion{% endblock %}

{% block body %}
    <h1>Explotacion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ explotacion.id }}</td>
            </tr>
            <tr>
                <th>Rexa</th>
                <td>{{ explotacion.rexa }}</td>
            </tr>
            <tr>
                <th>Roppi</th>
                <td>{{ explotacion.roppi }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('explotacion_index') }}">back to list</a>

    <a href="{{ path('explotacion_edit', {'id': explotacion.id}) }}">edit</a>

    {{ include('explotacion/_delete_form.html.twig') }}
{% endblock %}
