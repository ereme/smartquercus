{% extends 'base.html.twig' %}

{% block title %}Tratamiento{% endblock %}

{% block body %}
    <h1>Tratamiento</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tratamiento.id }}</td>
            </tr>
            <tr>
                <th>Registro</th>
                <td>{{ tratamiento.registro }}</td>
            </tr>
            <tr>
                <th>DosisRecomendada</th>
                <td>{{ tratamiento.dosisRecomendada }}</td>
            </tr>
            <tr>
                <th>Unidades</th>
                <td>{{ tratamiento.unidades }}</td>
            </tr>
            <tr>
                <th>NumAplicaciones</th>
                <td>{{ tratamiento.numAplicaciones }}</td>
            </tr>
            <tr>
                <th>DosisEmpleada</th>
                <td>{{ tratamiento.dosisEmpleada }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tratamiento_index') }}">back to list</a>

    <a href="{{ path('tratamiento_edit', {'id': tratamiento.id}) }}">edit</a>

    {{ include('tratamiento/_delete_form.html.twig') }}
{% endblock %}
